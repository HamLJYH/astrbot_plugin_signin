{
  "base_points": {
    "description": "每日签到基础积分",
    "type": "int",
    "default": 10,
    "hint": "每次签到获得的基础积分数"
  },
  "streak_bonus": {
    "description": "是否启用连续签到加成",
    "type": "bool",
    "default": true
  },
  "streak_bonus_rate": {
    "description": "连续签到加成比例",
    "type": "float",
    "default": 0.1,
    "hint": "每多连续1天增加的加成比例"
  },
  "max_streak_bonus": {
    "description": "最大连续签到加成倍数",
    "type": "float",
    "default": 2.0,
    "hint": "例如2.0表示最多2倍基础积分"
  },
  "top_limit": {
    "description": "排行榜显示人数",
    "type": "int",
    "default": 10
  },
  "reset_hour": {
    "description": "每日签到重置时间（小时）",
    "type": "int",
    "default": 5,
    "hint": "0-23，建议设为凌晨"
  },
  "enable_rank": {
    "description": "是否启用排行榜",
    "type": "bool",
    "default": true
  },
  "lucky_draw": {
    "description": "是否启用签到幸运奖励",
    "type": "bool",
    "default": true
  },
  "lucky_draw_points_max": {
    "description": "幸运奖励最大积分",
    "type": "int",
    "default": 50
  },
  "enable_shop": {
    "description": "是否启用积分商店",
    "type": "bool",
    "default": true
  },
  "enable_transfer": {
    "description": "是否启用积分转账",
    "type": "bool",
    "default": true
  },
  "transfer_min_amount": {
    "description": "最低转账积分",
    "type": "int",
    "default": 10
  },
  "transfer_fee_rate": {
    "description": "转账手续费率",
    "type": "float",
    "default": 0.05,
    "hint": "0.05表示5%手续费"
  },
  "transfer_cooldown": {
    "description": "转账冷却时间（秒）",
    "type": "int",
    "default": 300,
    "hint": "两次转账之间的最小间隔"
  }
}